## 请列举 SV39 页表页表项的组成，描述其中的标志位有何作用？

## 缺页
缺页指的是进程访问页面时页面不在页表中或在页表中无效的现象，此时 MMU 将会返回一个中断， 告知 os 进程内存访问出了问题。os 选择填补页表并重新执行异常指令或者杀死进程。

### 请问哪些异常可能是缺页导致的？

根据文档 Volume II: RISC-V Privileged Architectures V20211203，Table 3.6: Machine cause register (mcause) values after trap

有这些 trap 可能是因为缺页：instruction、load/store/AMO page-fault and access-fault exceptions.

### 发生缺页时，描述相关重要寄存器的值，上次实验描述过的可以简略。


- `STVAL` 引起page fault的内存地址

- `SCAUSE` 引起page fault的原因类型

- `SEPC` 引起page fault时的程序计数器值，这表明了page fault在用户空间发生的位置

缺页有两个常见的原因，其一是 Lazy 策略，也就是直到内存页面被访问才实际进行页表操作。 比如，一个程序被执行时，进程的代码段理论上需要从磁盘加载到内存。但是 os 并不会马上这样做， 而是会保存 .text 段在磁盘的位置信息，在这些代码第一次被执行时才完成从磁盘的加载操作。

### 这样做有哪些好处？

- 在进程启动的时候不用立刻分配所需要的所有空间，可以更快的启动程序
- 用户可能会申请大于所需要的空间

其实，我们的 mmap 也可以采取 Lazy 策略，比如：一个用户进程先后申请了 10G 的内存空间， 然后用了其中 1M 就直接退出了。按照现在的做法，我们显然亏大了，进行了很多没有意义的页表操作。

### 处理 10G 连续的内存页面，对应的 SV39 页表大致占用多少内存 (估算数量级即可)？

10G = 0x10 000 000 000 字节
    = 0x10 000 000 页
    = 0x2 000 000 三级页表
    = 0x400 000 二级页表
    = 0x80 000 一级页表

### 请简单思考如何才能实现 Lazy 策略，缺页时又如何处理？描述合理即可，不需要考虑实现。

在 mmap 的时候只填充 maparea 的 range ，但是不要 map 所有页面，等到中断的时候判断是不是 maparea 中已经标记的页面，再分配。


缺页的另一个常见原因是 swap 策略，也就是内存页面可能被换到磁盘上了，导致对应页面失效。

### 此时页面失效如何表现在页表项(PTE)上？

Volume II: RISC-V Privileged Architectures V20211203 Figure 4.21: Sv39 page table entry.
V 会变为 0，但是其余项目保留来表示是 swap 的页？

## 双页表与单页表

为了防范侧信道攻击，我们的 os 使用了双页表。但是传统的设计一直是单页表的，也就是说， 用户线程和对应的内核线程共用同一张页表，只不过内核对应的地址只允许在内核态访问。 (备注：这里的单/双的说法仅为自创的通俗说法，并无这个名词概念，详情见 KPTI )

### 在单页表情况下，如何更换页表？

在进程切换时候更换 satp。

### 单页表情况下，如何控制用户态无法访问内核页面？（tips:看看上一题最后一问）

看 U 这个标志位

### 单页表有何优势？（回答合理即可）

不用在陷入内核态的时候换页表，效率高

### 双页表实现下，何时需要更换页表？假设你写一个单页表操作系统，你会选择何时更换页表（回答合理即可）？

双页表：陷入内核、开启新进程，进程切换
单页表：进程切换，开启新进程